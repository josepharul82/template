<?xml version="1.0" encoding="UTF-8"?>
<hazelcast xsi:schemaLocation=
                   "http://www.hazelcast.com/schema/config http://www.hazelcast.com/schema/config/hazelcast-config-4.1.xsd"
           xmlns="http://www.hazelcast.com/schema/config"
           xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">

    <map name="userInfo">
        <time-to-live-seconds>315360000</time-to-live-seconds> <!-- Maximum time in seconds for each entry to stay in the map -->
        <max-idle-seconds>14400</max-idle-seconds> <!-- Maximum time in seconds for each entry to stay idle in the map. 4 hours here -->
        <eviction eviction-policy="LFU" size="20000"/> <!-- The entries least frequently used will be evicted -->
        <!-- <eviction-percentage>10</eviction-percentage>  When max-size is reached, the specified percentage of the map will be evicted. -->
    </map>

    <network>
        <port auto-increment="true" port-count="100">5701</port>
        <join>
            <multicast enabled="false">
            </multicast>
            <tcp-ip enabled="true">
                <member>localhost</member>
                <member>localhost</member>
            </tcp-ip>
        </join>
    </network>
</hazelcast>